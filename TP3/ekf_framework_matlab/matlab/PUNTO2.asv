close all
clear all


m = 0.5*ones(1,21);
c = 0:10:200;
mediciones = [101, 82, 91, 112, 99, 151, 96, 85, 99, 105];
prior = 0.5;
l = zeros(1,21);

for i = 1:size(mediciones)
    for j = 1:size(c)
        l(j) = l(j) + inv_sensor_model(m(i),j);
    end
end



function l = inv_sensor_model(m, x, z)
    
end
